# -*- coding: utf-8 -*-
"""polynomialRegression.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1MyALycIoNdw8nxktn4zBeM2sdhDSxKFX
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

"""Veri yukleme"""

veriler = pd.read_csv('/content/maaslar.csv')

X = veriler.iloc[:, [1]]
Y = veriler.iloc[:, [2]]

x = X.values
y = Y.values

"""Kiyaslama icin (for comparing)


LINEAR REGRESSION
"""

from sklearn.linear_model import LinearRegression

lin_reg = LinearRegression()
lin_reg.fit(X, Y)

plt.scatter(X, Y, c = 'green')
plt.plot(X, lin_reg.predict(X), c = 'darkblue')
plt.show()

"""POLYNOMIAL REGRESSION"""

from sklearn.preprocessing import PolynomialFeatures

# verileri derecesine gore olusturduk
poly_reg = PolynomialFeatures(degree = 4)

x_poly = poly_reg.fit_transform(x)

# polynomial predict
lin_regP = LinearRegression()
lin_regP.fit(x_poly, Y) # x_poly derecesine gore yeni X leri tanimlayan dizi
predY = lin_regP.predict(x_poly)

predY

plt.scatter(X, Y, c = 'red')
plt.plot(X, predY, c = 'b')
plt.show()

lin_regP.predict(poly_reg.fit_transform([[9.4]]))

